{
  "name": "hoahoconga-realtime",
  "version": "1.0.0",
  "description": "HoaHocCoNga.Com realtime gateway (Socket.IO). Relays events only — no business logic (see PROJECT.md REALTIME ARCHITECTURE).",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "socket.io": "^4.7.5"
  }
}
